#include <bits/stdc++.h>
using namespace std;

bool EsPrimo(int num);

int main() {
    int N;
    cin >> N;

    vector<int> primos;

    for (int i = 2; i <= N; i++) {
        if (EsPrimo(i)) {
            primos.push_back(i);
        }
    }
    for (int i = 0; i < primos.size(); i++) {
        cout << primos[i] << endl;
    }
    return 0;
}

bool EsPrimo(int num) {
    if (num <= 1) {
        return false;
    }
    for (int i = 2; i * i <= num; i++) {
        if (num % i == 0) {
            return false;
        }
    }
    return true;
}
